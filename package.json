{
  "name": "hulajdusza",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 execute hulajdusza --local --file=schema.sql",
    "db:remote": "wrangler d1 execute hulajdusza --remote --file=schema.sql",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250910.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
